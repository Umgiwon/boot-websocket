server:
  port: 8080

spring:
  application:
    name: grc

  profiles:
    active: dev

  web:
    resources:
      static-locations: classpath:/static/

app:
  api-key: "miro-grc-api-key"


# admin pwd
device:
  admin-password: 1234